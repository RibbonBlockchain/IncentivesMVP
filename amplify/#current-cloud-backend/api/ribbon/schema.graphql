type Patient @model {
  id: ID!
  phone: Int!
  countryCode: Int!
  events: [Event] @connection(name: "PatientEvents")
}
type Event @model {
  id: ID!
  eventType: String!
  patient: Patient @connection(name: "PatientEvents")
}